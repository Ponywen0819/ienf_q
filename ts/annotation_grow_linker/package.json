{
  "name": "annotation-grow-linker",
  "version": "0.1.0",
  "description": "TypeScript port of the AnnotationGrowLinker orchestrator (neural fiber reconstruction via annotation expansion).",
  "private": true,
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit",
    "build": "tsc -p tsconfig.build.json"
  },
  "devDependencies": {
    "@types/node": "^25.9.1",
    "typescript": "^5.6.3",
    "vitest": "^2.1.4"
  }
}
